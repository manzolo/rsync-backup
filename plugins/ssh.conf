# SSH - Keys and configuration
ENABLED=yes

PATH $HOME/.ssh
EXCLUDE known_hosts.old
RESTORE_EXCLUDE known_hosts

RESTORE_CMD chmod 700 $HOME/.ssh && find $HOME/.ssh -name "id_*" ! -name "*.pub" -exec chmod 600 {} \; && chmod 600 $HOME/.ssh/authorized_keys 2>/dev/null || true
